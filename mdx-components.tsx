import type { MDXComponents } from "mdx/types";

// Server component compatible MDX components
export function useMDXComponents(
  components: MDXComponents = {}
): MDXComponents {
  return components;
}
